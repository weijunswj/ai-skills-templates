# Skill Safety Matrix

This matrix is the maintenance review surface for deciding whether a skill should be used, extended, or split into a new canonical skill surface.

It does not grant permission to run risky actions. Repo rules, skill instructions, explicit user approval requirements, and source-of-truth ownership still apply.

## How To Use

- Before adding a new skill, adapter, template, or contract, compare the proposed use case against the existing rows.
- Prefer extending an existing skill when the trigger, safety boundary, source model, and validation path fit cleanly.
- Create a new Toolkit skill directly under `skills/<skill-name>/` after Skill Creation Center review, with coupled contracts, runtime, tests, and docs directly under canonical `repo/**` paths, only when the use case has a distinct trigger, domain, risk boundary, provenance requirement, local assets, or validation path. Do not add an intermediate ownership, publication, or generic synchronization layer.
- For third-party skills, skill packs, GitHub skill repos, or adapted external agent material, run `skill-product-review` before copying, installing, executing, or converting anything.
- The separate generic `skill-product-review` product retains its publication and anti-drift semantics; it is not part of Toolkit operation.

## Legend

| Risk Class | Meaning |
| --- | --- |
| Low | Instruction or review workflow with no default local writes, no bundled executable tooling, and no live-system mutation. |
| Medium | Local repo writes, local processes, generated surfaces, third-party provenance, or optional tools may be involved. |
| High | Credentials, workflow JSON, live systems, external service writes, CI/CD, Docker, deployments, or destructive impact are plausible if misused. |

## Skill Safety Matrix

| Skill | Primary Trigger | Risk Class | Local Writes | Scripts Or Tools | External Or Live Risk | Approval Boundary | Companion Skills | Source/Provenance | Notes And Boundaries |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| [skill-product-review](../../skills/skill-product-review/) | Third-party agent skill, `SKILL.md` folder, skill pack, or GitHub skill repo review before toolkit conversion. | Medium | Audit notes or repo docs only when requested. | None bundled. | Reads supplied or public source, but unsafe copied material is the core risk. | Approval required before copying, importing, installing, executing, or converting third-party material. | Direct-canonical Toolkit `skills/**` + `repo/**` maintenance; otherwise the target repository's documented workflow. | First-party direct-canonical skill; no third-party material is copied by the audit skill. | Mandatory gate for external skill material. Keep audit verdicts source-backed and conservative. |
| [repository-agent-rules](../../skills/repository-agent-rules/) | Bootstrap, check, repair, or refresh repo-local `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, and related agent-rule templates. | Medium | May write active instruction files in target repos and generated repo-local templates. | None bundled. | None by default, but instruction-file mutation affects future agent behavior. | Preview target writes and get explicit approval before mutating a consumer repo unless the scoped local repair was directly requested. | `n8n-safety-router` when the target is n8n-related. | First-party curated development module with retired internal source provenance. | Use once per unchecked target, then stop unless instruction-file state may have changed. |
| [toolkit-setup](../../skills/toolkit-setup/) | AI Agent Toolkit plugin setup, Toolkit Local Bridge setup or troubleshooting, repo-backed Toolkit auto-update, OpenCode bridge support, AG2 adapter support, bridge audit, enabled-target sync, disable, stale bridge state, native Codex or Claude Code plugin update behavior, Windows hook repair, or bridge setup safety. | Medium | May lead to user-level Toolkit bridge hub, managed clean `main` checkout fast-forward updates, Toolkit-managed report cleanup, OpenCode global skill, AG2 metadata, or installed-plugin hook wrapper writes only after explicit setup/profile selection or a directly requested install/update flow. | Routes literal `setup toolkit` to the managed checkout setup script when it exists (`%USERPROFILE%\.ai-agent-toolkit\source\ai-agent-toolkit\repo\scripts\setup-toolkit.cjs` or `$HOME/.ai-agent-toolkit/source/ai-agent-toolkit/repo/scripts/setup-toolkit.cjs`), with active `repo/scripts/setup-toolkit.cjs --execute --profile auto-main` as bootstrap/fallback only, then to `repo/scripts/toolkit-local-bridge.cjs` and `repo/scripts/repair-codex-plugin-windows-hooks.cjs` for scoped operations. | User-level setup state, optional hooks, stale bridge state, managed Toolkit source checkout state, installed plugin hook metadata, and native plugin update behavior can affect local agent environments. | One upfront checklist. OpenCode/AG2 remain opt-in. Explicit approval remains required before disabling targets, forcing downgrade, installing packages, mutating arbitrary project repos, touching credentials, or repairing an installed plugin root outside the current requested install/update flow. | Direct-canonical `skills/**` + `repo/**` maintenance; the standalone publisher is only for its own product. | First-party development module with no third-party source copied into the toolkit. | Single discoverability router only. Do not split into command-per-bridge skills or duplicate updater/repair implementation. |
| [codex-ssh-hostinger-coolify-setup-maintainer](../../skills/codex-ssh-hostinger-coolify-setup-maintainer/) | Codex SSH Hostinger VPS plus Coolify deployment setup, SSH preflight, daily security checks, intrusion-signal review, optional Telegram/email maintenance alerts, evidence-based maintenance, and incident response with owner approval gates. Use when the user asks Codex to help set up Hostinger for deployment, configure daily alerts, or review Hostinger/Coolify security signals. | High | May write repo-side evidence reports, approval logs, rollback plans, implementation notes, and placeholder-only docs under `docs/hostinger-coolify/` or a repo-documented Hostinger/Coolify docs path. Server-generated daily reports remain on the VPS unless the owner chooses otherwise. | Bundles checklists, evidence templates, OpenAI metadata, a read-only daily security report script with optional owner-configured notification delivery, a private env example, and a systemd timer installer script. | Hostinger VPS, Coolify, GitHub connections, DNS/TLS, real secrets, notification tokens, deployments, migrations, backups, restore tests, cutover, rollback, firewall changes, and reboots can affect live production systems. | Explicit approval required before billing/VPS changes, Coolify administration, GitHub live connection, DNS/TLS/proxy/firewall changes, real secrets, notification delivery setup, production deployments, migrations, backup/restore, cutover, rollback, reboot, or destructive actions. | `self-hosted-service-safety` for broader non-Coolify setup risk; `secure-ci-cd` for CI/CD deployment automation; `n8n-safety-router` if the target app is n8n; `n8n-environment-setup` for local n8n and the n8n-specific Hostinger Coolify VPS deployment reference after Coolify exists. | First-party development module with no copied third-party source; vendor docs used only as factual context. | Daily security script is read-only evidence collection plus optional outbound notification. Intrusion checks are signals only. Remediation remains manual and approval-gated. |
| [local-ai-safety](../../skills/local-ai-safety/) | Review local AI runtimes, model servers, model downloads, GPU/runtime setup, local AI web UIs, or local AI endpoint exposure. | Medium | Produces review notes or safe first-run plans only. | None bundled. | Installers, model pulls, Docker, GPU/runtime changes, local endpoints, public tunnels, telemetry, and prompt/file privacy can affect the device or network. | Explicit approval required before package installs, model downloads, Docker, driver/runtime changes, shell profile edits, credentials, public tunnels, or non-local endpoint exposure. | `n8n-safety-router` and `n8n-environment-setup` if the target is n8n. | First-party development module with no copied third-party source. | Review-only, not an installer. Do not invoke for ordinary AI API coding or prompt work. |
| [managed-app-foundation-review](../../skills/managed-app-foundation-review/) | Revisit implementation plans to compare low-cost, free, managed, or owner-hosted foundations before custom-building auth, backend APIs, user accounts, databases, workflow automation, CRM/contact pipelines, forms, email, storage, analytics, ops, traffic/security monitoring, or account-security foundations. | Medium | Produces planning notes only. | None bundled. | Provider account creation, billing, DNS, credentials, data migration, live configuration, paid-plan upgrades, live n8n actions, and stale pricing claims are the main risks. | Explicit approval required before creating accounts, configuring providers, entering secrets, changing DNS, migrating data, billing setup, paid upgrades, deployment, live-service mutation, or live n8n action. Current pricing/free-tier claims require official-source verification in the same turn. | `frontend-art-direction` for frontend form/security review; `self-hosted-service-safety` for public exposure/server review; `codex-ssh-hostinger-coolify-setup-maintainer` for Hostinger/Coolify maintenance; `n8n-safety-router` before n8n workflow work; `secure-ci-cd` for CI/CD setup. | First-party development module with no copied third-party source or pricing tables. | Review-only build-vs-buy gate. Do not freeze exact provider limits in the toolkit; use official docs when making concrete recommendations. Self-hosted n8n is only a candidate when compatible with n8n safety rules and owner-approved hosting. |
| [github-program-reconciler](../../skills/github-program-reconciler/) | Explicit-only current-main v5 canonical programme/bootstrap recovery plus N5 compatibility for parent/direct-child governance, deterministic bounded reconciliation, truthful PR-review inventory/finding disposition evidence, and parent-managed Deferred Findings. | High | May write only approved canonical skill surfaces during Toolkit development; runtime mutation requires injected, caller-authorised adapters. | One shared public facade, allowlisted v4/v5 state modules, contracts, and focused tests; no provider client, workflow, MCP, or live adapter bundled. | GitHub issue/PR bodies, review inventory, conversations, repository state, bootstrap state, and external writes can affect governance if authority or readback is incomplete. | Explicit current-turn approval and exact repository identity, A2 `repository.governance` consent, accepted preview, and A1 `github.mutation` authority are required for mutation. Programme Apply, bootstrap-file Apply, receipt persistence, review-thread mutation, disposition, Ready, merge, cleanup, and Web finality remain controller-owned. | Direct-canonical `skills/**` + `repo/**` maintenance; current GitHub skill for ordinary GitHub inspection; A1-A4 source modules for authority boundaries. | First-party development module with no third-party source copied or adapted. | Explicit-only and fail-closed. One shared facade, deterministic v5 state/projection, independent trusted scopes, current receipt separation, complete inspection/revision binding, immediate readback, byte/order proof, truthful inventory, and DF revalidation; no historical duplicate runtime or receipt schema. |
| [release-readiness-audit](../../skills/release-readiness-audit/) | Final audit, completion audit, production-readiness audit, release-candidate audit, launch-readiness audit, QA pass, "make sure everything works", "is this production ready", `/goal` readiness remediation, audit against original docs, security-readiness check, or final readiness check. | High | May create or update repo-local audit reports, then make scoped remediation diffs after the report exists. | None bundled; may use local validation, Playwright/browser tooling, and Codex Security if available and approved. | Broad validation, browser verification, security scans, external-service checks, deployment assumptions, customer/private data, and remediation can become high-impact if ungated. | Full audit requires explicit confirmation after lightweight preflight. Deep security scan and browser click-through require confirmation. Live, deployment, external-service, destructive, credential, payment, customer-data, private-data, import/export, or sync actions require separate explicit current-turn approval naming the target and action. | Codex Security skills/plugin if installed; `frontend-art-direction` for UI findings; `windows-local-dev-services` for local UI/API startup; `secure-ci-cd` for CI/CD release gates; `self-hosted-service-safety` or `codex-ssh-hostinger-coolify-setup-maintainer` for hosting risk; `n8n-safety-router` for n8n surfaces. | First-party development module with no copied third-party source, scanner output, browser artifacts, or vendor docs. | Metadata disables implicit invocation where supported. If implicitly routed, preflight only. Do not claim perfect security or production readiness when P0/P1, skipped/inaccessible validation, unavailable security scan, or unverified live/private-data areas remain. |
| [n8n-safety-router](../../skills/n8n-safety-router/) | Any n8n task, including [official n8n Skills](https://github.com/n8n-io/skills), their entry-point meta-skill currently named `using-n8n-skills`, workflow JSON, official n8n MCP, live n8n, credentials, import/export, validation, execution, activation, or repo/live sync. | High | May affect repo workflow JSON, ignored staging folders, adapters, or local helper outputs. | Includes an adapter installer script. | Live n8n, credentials, webhook IDs, workflow activation, execution, import/export, repo/live sync, MCP metadata, and MCP configs are high risk if trusted or executed blindly. | Explicit current-turn approval required for live n8n, Docker, import/export, sync, activation, execution, publish/unpublish, credential, deployment, production, destructive, privileged external actions, or live MCP inspection. Untrusted repo/PR MCP configs are static-review only until trusted and approved. | Required before `n8n-environment-setup`, `n8n-workflow-transport` when workflow or live work is involved. | First-party curated development module with retired internal source provenance. | This is the n8n safety router. Load it before planning or editing n8n work. |
| [n8n-environment-setup](../../skills/n8n-environment-setup/) | Safe n8n environment setup with Docker Compose, Postgres, localhost/ngrok dev stack, production Cloudflare Tunnel self-hosting stack for local/CGNAT machines, Hostinger Coolify VPS notes for hosted n8n, launchers, menus, [official n8n Skills](https://github.com/n8n-io/skills) setup, and official instance-level MCP references. | High | May copy local and production stack templates, `.env.example`, launchers, scripts, and MCP config examples. | Bundles Windows command and PowerShell helpers for the local dev stack and separate production Cloudflare stack. | Docker, ngrok tunnels, Cloudflare Tunnel, DNS, VPS setup, official instance-level MCP, credentials, production data, backups, and local service exposure can become live-system work. | Explicit approval required before Docker, live n8n, Cloudflare, DNS, tunnel, import/export, activation, execution, credential, deployment, VPS, backup/restore, or production actions. | `n8n-safety-router` before workflow, helper-script, or live n8n tasks. | n8n local-setup module with retired migration provenance plus first-party production Cloudflare stack additions. | Setup docs are safe to read. Running stack helpers, changing live setup, or performing Cloudflare/Docker/production actions is approval-gated. |
| [n8n-workflow-transport](../../skills/n8n-workflow-transport/) | Sanitizer helpers, import/export sync helpers, validation, comparison, live-import prep, and repo/live workflow hygiene. | High | May write ignored staging folders, workflow JSON, sanitized templates, or helper outputs in consumer repos. | Bundles `.cjs`, `.js`, `.ps1`, and `.cmd` helper templates. | Live import/export, credential comparison, workflow sync, and repo/live drift decisions are high risk. | Explicit approval required before live import/export, sync, activation, execution, credentials, or workflow mutation outside local dry-run review. | `n8n-safety-router` first. | n8n workflow-toolkit module with retired migration provenance. | Default to dry-run, sanitize, validate, and compare before any live action. |
| [self-hosted-service-safety](../../skills/self-hosted-service-safety/) | Review non-n8n self-hosted services, Docker/VPS setup, reverse proxies, public ports, DNS/TLS, tunnels, credentials, backups, public admin/backup paths, honeypot/canary paths, traffic logs, SSH access, or first-run hardening. | High | Produces review notes or safe first-run plans only. | None bundled. | Docker, VPS/cloud settings, DNS/TLS, tunnels, public ports, default credentials, admin panels, webhooks, logs, SSH, and service data can affect live systems. | Explicit approval required before Docker, deployment, DNS/TLS, firewall/public ports, tunnels, VPS/cloud mutation, credentials, production data, SSH configuration, or backup/restore actions. | `n8n-safety-router` and `n8n-environment-setup` if the target is n8n. | First-party development module with no copied third-party source. | Review-only, not a deployment playbook. Honeypot/canary paths must be inert and credential-free. Do not invoke for ordinary app coding or local-only dev servers. |
| [secure-ci-cd](../../skills/secure-ci-cd/) | CI/CD setup, GitHub Actions, pipeline hardening, required checks, branch protection, release automation, deployment automation, and safe rollout planning. | High | May create local workflow templates, docs-based status files under `docs/ci-cd/` or a repo-documented docs path, or implementation plans. | None bundled. | CI/CD, branch protection, releases, deployments, and repository settings can mutate external systems. | Explicit approval required before commit, push, PR, merge, deploy, settings, secret, release, or other external mutation unless repo-local Git Completion rules explicitly scope the action. | Direct-canonical `skills/**` + `repo/**` maintenance; the standalone publisher is only for its own product. | Secure installer module with retired migration provenance. | Keep secrets out of files. Treat deployment automation and required-check changes as safety-gated. |
| [frontend-art-direction](../../skills/frontend-art-direction/) | Frontend design systems, landing pages, dashboards, forms, accessibility, responsive polish, privacy-safe UX, and implementation review. | Medium | May produce local design recommendations, references, generated design-system outputs, or frontend edits when requested. | Bundles a local-only Python design-system generator and tests. DESIGN.md support is documentation-only by default. | Third-party source attribution and optional frontend implementation can affect product surfaces. | Approval required before installing packages, fetching network assets, changing production UI, running uncertain third-party code, overwriting `DESIGN.md`, or invoking upstream Google DESIGN.md CLI/package tooling. | Direct-canonical `skills/**` + `repo/**` maintenance; the standalone publisher is only for its own product. | Active third-party source locks for `nextlevelbuilder/ui-ux-pro-max-skill` and `google-labs-code/design.md`; public attribution required and manual review required. | Generator must remain local-only and CSV-backed. DESIGN.md remains a design contract, not a replacement for security/privacy/accessibility checks. Preserve attribution and guardrails. |
| [windows-local-dev-services](../../skills/windows-local-dev-services/) | Start, relaunch, verify, or troubleshoot Windows localhost web apps, API servers, and dev services. | Medium | May create or read local logs and launch local dev servers when requested. | None bundled. | Local ports, background processes, execution policy, package managers, and browser verification can affect the developer machine. | Approval required for destructive cleanup, package installation, privileged process changes, or non-local network exposure. | None required. | First-party development module with retired internal source provenance. | Keep targets on localhost unless explicitly approved. Verify with logs and health checks before claiming success. |

## Description Review Notes

OpenAI/Codex metadata permits implicit discovery for the 12 products admitted by `repo/tests/fixtures/skill-routing-cases.json`. `codex-ssh-hostinger-coolify-setup-maintainer`, `github-program-reconciler`, and `release-readiness-audit` remain explicit-only. Implicit discovery never grants mutation or live-system authority.

Watch these trigger boundaries when editing descriptions:

- `n8n-safety-router` should stay the broad mandatory n8n safety gate.
- `n8n-environment-setup` should stay focused on environment setup, the localhost/ngrok dev path, the separate production Cloudflare Tunnel path for local/CGNAT machines, hosted n8n Hostinger Coolify VPS notes, [official n8n Skills](https://github.com/n8n-io/skills) setup, and official instance-level MCP references.
- `n8n-workflow-transport` should stay focused on helper templates and repo/live hygiene.
- Test-only n8n workflow fixtures must not regain a public product, route, install surface, or reusable-template claim.
- `skill-product-review` should stay focused on third-party product review and approved provenance-preserving publication, not general coding work or Toolkit topology.
- `codex-ssh-hostinger-coolify-setup-maintainer` should stay focused on Hostinger VPS plus Coolify deployment setup, security checks, intrusion-signal review, maintenance alerts, evidence-based maintenance, and incident response, not generic DevOps or n8n app deployment inside Coolify after the platform exists.
- `release-readiness-audit` should stay a guarded final readiness audit and remediation loop, not ordinary implementation, narrow code review, deployment execution, or an implicit full-repo scan.
- `local-ai-safety` and `self-hosted-service-safety` should stay small, review-only, and scoped to setup/exposure risk.
- `github-program-reconciler` should stay explicit-only, current-main scoped, and separate from ordinary GitHub inspection, Web finality, review mutation, and A1-A4 authority.

## Maintenance

- Update this doc when a skill is added, removed, renamed, materially changes trigger scope, gains tools, changes provenance, or changes approval boundaries.
- Keep `repo/docs/FOR_AI_AGENTS.md`, README skill tables, and `repo/contracts/agent-rules/toolkit-skill-routing.md` aligned when skill routing changes.
- Run `node --test repo/tests/skill-routing.test.cjs` after matrix edits.
- Run `node repo/scripts/validate-toolkit.cjs` before pushing.
